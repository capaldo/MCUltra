$(document).ready(function() {
    $(".header").html(function(index, currentHtml) {
        return currentHtml.replace(/ /gi, " | ");
    });
});

changeText = function(text, newText){
  var currentText = $('.header').html();
    $('.header').html(currentText.replace(text,newText));
};
window.setTimeout(function(){changeText('ImageQAEnhanced', '<div class="symbols">&#xf7eb;</div>')}, 0)

changeText = function(text, newText){
  var currentText = $('.header').html();
    $('.header').html(currentText.replace(text,newText));
};
window.setTimeout(function(){changeText('Image Occlusion Enhanced', '<div class="symbols">&#xf7eb;</div>')}, 0)

changeText = function(text, newText){
  var currentText = $('.header').html();
    $('.header').html(currentText.replace(text,newText));
};
window.setTimeout(function(){changeText('MCAT::LowYield', '<div class="symbols">&#xf103;</div>')}, 0)

changeText = function(text, newText){
  var currentText = $('.header').html();

    $('.header').html(currentText.replace(text,newText));
};

window.setTimeout(function(){changeText('::', ' ‣ ')}, 0)

changeText = function(text, newText){
  var currentText = $('.header').html();

    $('.header').html(currentText.replace(text,newText));
};

window.setTimeout(function(){changeText('::', ' ‣ ')}, 0)

changeText = function(text, newText){
  var currentText = $('.header').html();

    $('.header').html(currentText.replace(text,newText));
};


window.setTimeout(function(){changeText('::', ' ‣ ')}, 0)

changeText = function(text, newText){
  var currentText = $('.header').html();

    $('.header').html(currentText.replace(text,newText));
};

window.setTimeout(function(){changeText('::', ' ‣ ')}, 0)

